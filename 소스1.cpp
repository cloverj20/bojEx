#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int n;
vector<int> lope(n);


int main() {
    cin >> n;
    for (int i = 0; i < n; i++) {
        int input;
        cin >> input;
        lope.push_back(input);
    }
    int temp[100000] ={0};
    int maxW = 0;
    for (int i = 0; i < n; i++) {
        
    }
    
    return 0;
}